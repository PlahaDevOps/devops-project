# Start from Ubuntu base image
FROM ubuntu:20.04

# Install curl (or anything lightweight)
RUN apt-get update && apt-get install -y curl

# Default command when the container runs
CMD ["echo", "🎉 Hello from your first Docker container!"]
